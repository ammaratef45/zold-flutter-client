
import 'package:flutter/material.dart';

/// page shown when device isn't trusted
class UntrustedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) =>
    Center(child: const Text('This device is not trustable'),);

}