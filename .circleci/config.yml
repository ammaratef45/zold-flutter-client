version: 2
jobs:
  build:
    docker:
      - image: cirrusci/flutter
    steps:
      - checkout
      - run: flutter doctor
      - run: cd zold_wallet && flutter packages get 
      - run: cd zold_wallet && flutter test 
      - run: cd zold_wallet && flutter -v build apk
